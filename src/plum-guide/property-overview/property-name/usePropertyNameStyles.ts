import { makeStyles } from '@material-ui/core';

export const usePropertyNameStyles = makeStyles(() => {
    return {
        propertyName: {
            fontSize: '40px',
            marginTop: '40px',
            marginBottom: '10px',
        }
    }
});
